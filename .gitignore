node_modules
dist
dist-ssr
coverage
*.local

.DS_Store
Thumbs.db

logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

test-results
playwright-report
blob-report

.claude

# Some editor extensions transpile TypeScript next to its source. Nothing in
# src/ is authored as JavaScript, so anything that appears there is build junk.
src/**/*.js
src/**/*.js.map
src/**/*.d.ts
!src/vite-env.d.ts

bench/dist

# The working output of a bench run. Named snapshots next to it are committed;
# this one is overwritten every time and would only produce noisy diffs.
bench/results/latest.json
